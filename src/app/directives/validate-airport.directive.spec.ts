import { ValidateAirportDirective } from './validate-airport.directive';

describe('ValidateAirportDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAirportDirective();
    expect(directive).toBeTruthy();
  });
});
